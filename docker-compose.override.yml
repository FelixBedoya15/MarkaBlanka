# Please consult our docs for more info: https://www.librechat.ai/docs/configuration/docker_override

# TO USE THIS FILE, FIRST UNCOMMENT THE LINE ('services:')

# THEN UNCOMMENT ONLY THE SECTION OR SECTIONS CONTAINING THE CHANGES YOU WANT TO APPLY
# SAVE THIS FILE AS 'docker-compose.override.yaml'
# AND USE THE 'docker compose build' & 'docker compose up -d' COMMANDS AS YOU WOULD NORMALLY DO

# WARNING: YOU CAN ONLY SPECIFY EVERY SERVICE NAME ONCE (api, mongodb, meilisearch, ...)
# IF YOU WANT TO OVERRIDE MULTIPLE SETTINGS IN ONE SERVICE YOU WILL HAVE TO EDIT ACCORDINGLY

# EXAMPLE: if you want to use the config file and the latest numbered release docker image the result will be:

# services:
#   api:
#     volumes:
#     - type: bind
#       source: ./somossst.yaml
#       target: /app/somossst.yaml
#     image: ghcr.io/danny-avila/librechat:latest

# ---------------------------------------------------

# Agregamos el bind mount para el código fuente (./) a /app
version: '3.4'
services:
  # === 1. SERVICIO DE LA API (Cambios de Código) ===
  api:
    # AÑADE ESTA LÍNEA CRÍTICA.
    env_file:
      - .env
    # Agregamos el bind mount para el código fuente (./) a /app
    volumes:
      #- type: bind
      #  source: ./
      #  target: /app
      # MANTENER los bind mounts ya definidos en el .yml, si quieres asegurar que no se pierdan
      # aunque docker compose ya debería fusionar los volúmenes correctamente.
      # Para estar 100% seguros y si tienes problemas de fusión de volúmenes:
      #- type: bind
      #  source: ./.env
      #  target: /app/.env
      #- ./uploads:/app/uploads
      #- ./logs:/app/logs
      # Si también quieres usar el archivo de configuración somossst.yaml (opcional, como en tu ejemplo)
      #- ./somossst.yaml:/app/somossst.yaml

      # === 2. SERVICIO DE MONGODB (Persistencia de Datos) ===
  somossst-mongodb:
    # Sobrescribimos la versión de la imagen si lo deseas
    image: mongo:4.4.18
    volumes:
      # CAMBIO CRÍTICO: Sobrescribe el ./data-node:/data/db con un volumen nombrado
      # Esto garantizará que la data persista entre re-despliegues de Dokploy.
      - mongodb_data:/data/db

  # === 3. SERVICIO DE OLLAMA (IA Local) ===
  ollama:
    image: ollama/ollama:latest
    container_name: somossst-ollama
    restart: always
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - dokploy-network

  # === 4. AUTO-PULL MODELS (Descarga automática de modelos) ===
  pull-models:
    image: curlimages/curl:latest
    container_name: somossst-pull-models
    networks:
      - dokploy-network
    depends_on:
      - ollama
    entrypoint: [ "/bin/sh", "-c" ]
    command: >
      " sleep 10; curl -X POST http://ollama:11434/api/pull -d '{\"name\": \"deepseek-r1:1.5b\"}'; curl -X POST http://ollama:11434/api/pull -d '{\"name\": \"gemma2:2b\"}'; curl -X POST http://ollama:11434/api/pull -d '{\"name\": \"qwen2:0.5b\"}'; "

# === 3. VOLUMENES NOMBRADOS (Necesario para la persistencia) ===
volumes:
  mongodb_data:
  ollama_data:

    # version: '3.4'
    # services:

    # # USE SOMOSSST CONFIG FILE
    #   api:
    #     volumes:
    #     - type: bind
    #       source: ./somossst.yaml
    #       target: /app/somossst.yaml

    #  mongodb:
    #     image: mongo:4.4.18

    # # LOCAL BUILD
    #   api:
    #     image: librechat
    #     build:
    #       context: .
    #       target: node

    # # BUILD FROM LATEST IMAGE
    #   api:
    #     image: ghcr.io/danny-avila/librechat-dev:latest

    # # BUILD FROM LATEST IMAGE (NUMBERED RELEASE)
    #   api:
    #     image: ghcr.io/danny-avila/librechat:latest

    # # BUILD FROM LATEST API IMAGE
    #   api:
    #     image: ghcr.io/danny-avila/librechat-dev-api:latest

    # # BUILD FROM LATEST API IMAGE (NUMBERED RELEASE)
    #   api:
    #     image: ghcr.io/danny-avila/librechat-api:latest

    # # ADD SAML CERT FILE
    #   api:
    #     volumes:
    #     - type: bind
    #       source: ./your_cert.pem
    #       target: /app/your_cert.pem

    # # ADD MONGO-EXPRESS
    #   mongo-express:
    #     image: mongo-express
    #     container_name: mongo-express
    #     environment:
    #       ME_CONFIG_MONGODB_SERVER: mongodb
    #       ME_CONFIG_BASICAUTH_USERNAME: admin
    #       ME_CONFIG_BASICAUTH_PASSWORD: password
    #     ports:
    #       - '8081:8081'
    #     depends_on:
    #       - mongodb
    #     restart: always

    # # USE MONGODB V4.4.18 - FOR OLDER CPU WITHOUT AVX SUPPORT
    #   mongodb:
    #     image: mongo:4.4.18

    # # DISABLE THE MONGODB CONTAINER - YOU NEED TO SET AN ALTERNATIVE MONGODB URI IN THE .ENV FILE
    #   api:
    #     environment:
    #       - MONGO_URI=${MONGO_URI}
    #   mongodb:
    #     image: tianon/true
    #     command: ""
    #     entrypoint: ""

    # # EXPOSE MONGODB PORTS - USE CAREFULLY, THIS MAKES YOUR DATABASE VULNERABLE TO ATTACKS
    #   mongodb:
    #     ports:
    #       - 27018:27017

    # # DISABLE MEILISEARCH
    #   meilisearch:
    #     profiles:
    #       - donotstart

    # # EXPOSE MEILISEARCH PORTS - DO NOT USE THE DEFAULT VALUE FOR THE MASTER KEY IF YOU DO THIS
    #   meilisearch:
    #     ports:
    #       - 7700:7700

    # # USE RAG API IMAGE WITH LOCAL EMBEDDINGS SUPPORT
    #  rag_api:
    #    image: ghcr.io/danny-avila/librechat-rag-api-dev:latest
    # # For Linux user:
    #    extra_hosts:
    #      - "host.docker.internal:host-gateway"

    # # ADD OLLAMA
    #  ollama:
    #    image: ollama/ollama:latest
    #    deploy:
    #      resources:
    #        reservations:
    #          devices:
    #            - driver: nvidia
    #              capabilities: [compute, utility]
    #    ports:
    #      - "11434:11434"
    #    volumes:
    #      - ./ollama:/root/.ollama

    # # ADD LITELLM BASIC - NEED TO CONFIGURE litellm-config.yaml, ONLY NEED ENV TO ENABLE REDIS FOR CACHING OR LANGFUSE FOR MONITORING
    #  litellm:
    #    image: ghcr.io/berriai/litellm:main-latest
    #    volumes:
    #      - ./litellm/litellm-config.yaml:/app/config.yaml
    #      - ./litellm/application_default_credentials.json:/app/application_default_credentials.json # only if using Google Vertex
    #    ports:
    #      - "4000:8000"
    #    command: [ "--config", "/app/config.yaml", "--port", "8000", "--num_workers", "8" ]
    #    environment:
    #      OPENAI_API_KEY: none ## needs to be set if ollama's openai api compatibility is used
    #      GOOGLE_APPLICATION_CREDENTIALS: /app/application_default_credentials.json ## only if using Google Vertex
    #      REDIS_HOST: redis
    #      REDIS_PORT: 6379
    #      REDIS_PASSWORD: RedisChangeMe
    #      LANGFUSE_PUBLIC_KEY: pk-lf-RandomStringFromLangfuseWebInterface
    #      LANGFUSE_SECRET_KEY: sk-lf-RandomStringFromLangfuseWebInterface
    #      LANGFUSE_HOST: http://langfuse-server:3000

    # # ADD LITELLM CACHING
    #  redis:
    #    image: redis:7-alpine
    #    command:
    #    - sh
    #    - -c # this is to evaluate the $REDIS_PASSWORD from the env
    #    - redis-server --appendonly yes --requirepass $$REDIS_PASSWORD ## $$ because of docker-compose
    #    environment:
    #      REDIS_PASSWORD: RedisChangeMe
    #    volumes:
    #    - ./redis:/data

    # # ADD LITELLM MONITORING
    #  langfuse-server:
    #    image: ghcr.io/langfuse/langfuse:latest
    #    depends_on:
    #      - db
    #    ports:
    #      - "3000:3000"
    #    environment:
    #      - NODE_ENV=production
    #      - DATABASE_URL=postgresql://postgres:PostgresChangeMe@db:5432/postgres
    #      - NEXTAUTH_SECRET=ChangeMe
    #      - SALT=ChangeMe
    #      - NEXTAUTH_URL=http://localhost:3000
    #      - TELEMETRY_ENABLED=${TELEMETRY_ENABLED:-true}
    #      - NEXT_PUBLIC_SIGN_UP_DISABLED=${NEXT_PUBLIC_SIGN_UP_DISABLED:-false}
    #      - LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES=${LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES:-false}
    #  db:
    #    image: postgres
    #    restart: always
    #    environment:
    #      - POSTGRES_USER=postgres
    #      - POSTGRES_PASSWORD=PostgresChangeMe
    #      - POSTGRES_DB=postgres
    #    volumes:
    #      - ./postgres:/var/lib/postgresql/data
